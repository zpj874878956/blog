---
title: 使用openssl创建自签发证书
date: 2024-10-22 16:08:52
permalink: /pages/f7d9ee/
categories:
  - 运维
  - linux
tags:
  - 
feed:
  enable: true
---

# 生成根证书

## 生成ca秘钥，得到ca.key

```bash
openssl genrsa -out ca.key 4096
```

## 生成ca证书签发请求，得到ca.csr

```bash
openssl req -new -key ca.key -out ca.csr -subj "/C=CN/ST=GD/L=ShenZhen/O=other/OU=other/CN=ca.com"
```

## 生成ca根证书，得到ca.crt

```bash
openssl x509 -req -days 3650 -in ca.csr -signkey ca.key -out ca.crt
```

# 生成服务端证书

## 生成秘钥，得到server.Key

```bash
openssl genrsa -out server.key 2048
```

## 服务端CSR

```bash
openssl req -new -sha256 -out server.csr -key server.key -subj "/C=CN/ST=GD/L=ShenZhen/O=other/OU=other/CN=172.16.8.10"
```

## 生成自签名SSL证书

```bash
openssl x509 -req -days 3650 -in server.csr -signkey server.key -out server.crt
```

客户端导入ca.crt 公钥
